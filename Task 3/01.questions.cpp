    /**
     * a) a and b will never make p4 run for all numerical and boolean values assigned to a and b
     * b) (a < 10) and (b < 10);
     * c) T; cond ? (S;) : (U;);
     * d) cond = x;
     * e) if((age >= 18) and (age < 65)) {state = ACTIVE;} else {state = (age < 18) ? MINOR : RETIRED}
     * f)
     * 1) true
     * 2) true
     * 3) true
     * 4) false
     * 5) false
     * 6) false
     */